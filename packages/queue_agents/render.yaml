projects:
- name: yuanqiqi882-svg-agents-workshop-queue
  environments:
  - name: production
    databases:
    - name: yuanqiqi882-svg-queue-agents-db
      plan: basic-256mb
      region: oregon
      postgresMajorVersion: '18'
    services:
    - type: keyvalue
      name: yuanqiqi882-svg-queue-agents-valkey
      plan: starter
      region: oregon
      maxmemoryPolicy: noeviction
      ipAllowList: []
    - type: web
      name: yuanqiqi882-svg-queue-agents-web
      runtime: python
      region: oregon
      plan: starter
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.web
      healthCheckPath: /healthz
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: yuanqiqi882-svg-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: yuanqiqi882-svg-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
    - type: worker
      name: yuanqiqi882-svg-queue-agents-worker
      runtime: python
      region: oregon
      plan: starter
      numInstances: 1
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.worker
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: yuanqiqi882-svg-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: yuanqiqi882-svg-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
